全局 vs 专属：
内核全局页表（init_mm.pgd）：所有进程共享的内核空间映射；
进程专属页表（mm->pgd）：每个进程独有的用户空间映射；

init_task
video_devices
jiffies

cdev_map
(gdb) p /x cdev_map.probes[5].next.next.dev
$58 = 0x500000
(gdb) p /x cdev_map.probes[5].next.dev
$59 = 0x500001
(gdb) p /x cdev_map.probes[5].dev
$60 = 0x500002
////////////////////////////

node_data
contig_page_data

slab_caches
ping_table

init_net
inet_addr_lst
arp_tbl
